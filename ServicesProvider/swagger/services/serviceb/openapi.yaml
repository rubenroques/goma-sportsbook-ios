openapi: 3.0.0
info:
  title: Service B API
  description: Example Service B with dummy endpoint
  version: 0.0.1
  contact:
    name: API Support
    email: support@example.com

servers:
  - url: https://serviceb.example.com
    description: Production server
  - url: https://serviceb-stg.example.com
    description: Development server

tags:
  - name: Example
    description: Example endpoints

components:
  schemas:
    DummyResponse:
      type: object
      properties:
        message:
          type: string
        timestamp:
          type: string
          format: date-time

paths:
  /hello:
    get:
      tags:
        - Example
      summary: Hello World endpoint
      description: A simple endpoint that returns a greeting
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DummyResponse' 