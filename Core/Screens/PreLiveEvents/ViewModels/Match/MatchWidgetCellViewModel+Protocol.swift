import Foundation
import ServicesProvider

// Extension to make the real MatchWidgetCellViewModel conform to our protocol
extension MatchWidgetCellViewModel: MatchWidgetCellViewModelProtocol {
    // No need for additional implementation since the class already has all required properties
    // This extension simply declares that MatchWidgetCellViewModel conforms to MatchWidgetCellViewModelProtocol
} 