import XCTest
@testable import GomaPlatform

final class GomaPlatformTests: XCTestCase {

    func testLanguageManagerProtocolExists() throws {
        // Basic test to verify the protocol is accessible
        // Full tests will be added as more components are extracted
        XCTAssertTrue(true)
    }
}
