# Example configuration for Swift Code Migration Validator
# -----------------------------------------------

# Path to the original monolithic Swift file
old_file: "Core/Screens/PreLiveEvents/Views/Cells/Match/MatchWidgetCollectionViewCell_old.swift"

# Option 1: List of specific extension files
new_files: 
  - "Core/Screens/PreLiveEvents/Views/Cells/Match/MatchWidget/Extensions/MatchWidgetCollectionViewCell+Layout.swift"
  - "Core/Screens/PreLiveEvents/Views/Cells/Match/MatchWidget/Extensions/MatchWidgetCollectionViewCell+Setup.swift"
  - "Core/Screens/PreLiveEvents/Views/Cells/Match/MatchWidget/Extensions/MatchWidgetCollectionViewCell+Actions.swift"
  - "Core/Screens/PreLiveEvents/Views/Cells/Match/MatchWidget/Extensions/MatchWidgetCollectionViewCell+Theme.swift"

# Option 2 (alternative): Use a glob pattern to match all extension files
# Uncomment the line below and comment out the list above to use this pattern
# new_files: "Core/Screens/PreLiveEvents/Views/Cells/Match/MatchWidget/Extensions/MatchWidgetCollectionViewCell+*.swift"

# Lines to ignore during comparison (optional)
# These can be exact matches or patterns with wildcards (*pattern*)
ignore_lines:
  # Common imports that may appear in multiple files
  - "import UIKit"
  - "import Foundation"
  
  # Common Swift syntax elements that may be formatted differently
  - "{"
  - "}"
  - "}"
  - "extension MatchWidgetCollectionViewCell {"
  - "// MARK: -"
  
  # Methods that might have been intentionally removed or refactored
  - "*setNeedsLayout*"
  - "*layoutIfNeeded*" 